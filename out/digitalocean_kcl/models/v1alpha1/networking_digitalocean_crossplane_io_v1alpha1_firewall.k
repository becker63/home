"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Firewall:
    r"""
    Firewall is the Schema for the Firewalls API.

    Attributes
    ----------
    apiVersion : str, default is "networking.digitalocean.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Firewall", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpec, default is Undefined, required
        spec
    status : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatus, default is Undefined, optional
        status
    """


    apiVersion: "networking.digitalocean.crossplane.io/v1alpha1" = "networking.digitalocean.crossplane.io/v1alpha1"

    kind: "Firewall" = "Firewall"

    metadata?: v1.ObjectMeta

    spec: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpec

    status?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatus


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpec:
    r"""
    FirewallSpec defines the desired state of Firewall

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProvider, default is Undefined, required
        for provider
    initProvider : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProvider

    initProvider?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecProviderConfigRef

    publishConnectionDetailsTo?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecWriteConnectionSecretToRef


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProvider:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall spec for provider

    Attributes
    ----------
    dropletIds : [float], default is Undefined, optional
        The list of the IDs of the Droplets assigned
        to the Firewall.
    dropletIdsRefs : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsRefsItems0], default is Undefined, optional
        References to Droplet in droplet to populate dropletIds.
    dropletIdsSelector : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsSelector, default is Undefined, optional
        droplet ids selector
    inboundRule : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderInboundRuleItems0], default is Undefined, optional
        The inbound access rule block for the Firewall.
        The inbound_rule block is documented below.
    name : str, default is Undefined, optional
        The Firewall name
    outboundRule : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderOutboundRuleItems0], default is Undefined, optional
        The outbound access rule block for the Firewall.
        The outbound_rule block is documented below.
    tags : [str], default is Undefined, optional
        The names of the Tags assigned to the Firewall.
    """


    dropletIds?: [float]

    dropletIdsRefs?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsRefsItems0]

    dropletIdsSelector?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsSelector

    inboundRule?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderInboundRuleItems0]

    name?: str

    outboundRule?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderOutboundRuleItems0]

    tags?: [str]


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsRefsItems0:
    r"""
    A Reference to a named object.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsRefsItems0Policy, default is Undefined, optional
        policy
    """


    name: str

    policy?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsRefsItems0Policy


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsRefsItems0Policy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsSelector:
    r"""
    Selector for a list of Droplet in droplet to populate dropletIds.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsSelectorPolicy


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderDropletIdsSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderInboundRuleItems0:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall spec for provider inbound rule items0

    Attributes
    ----------
    portRange : str, default is Undefined, optional
        The ports on which traffic will be allowed
        specified as a string containing a single port, a range (e.g. "8000-9000"),
        or "1-65535" to open all ports for a protocol. Required for when protocol is
        tcp or udp.
    $protocol : str, default is Undefined, optional
        The type of traffic to be allowed.
        This may be one of "tcp", "udp", or "icmp".
    sourceAddresses : [str], default is Undefined, optional
        An array of strings containing the IPv4
        addresses, IPv6 addresses, IPv4 CIDRs, and/or IPv6 CIDRs from which the
        inbound traffic will be accepted.
    sourceDropletIds : [float], default is Undefined, optional
        An array containing the IDs of
        the Droplets from which the inbound traffic will be accepted.
    sourceKubernetesIds : [str], default is Undefined, optional
        An array containing the IDs of
        the Kubernetes clusters from which the inbound traffic will be accepted.
    sourceLoadBalancerUids : [str], default is Undefined, optional
        An array containing the IDs
        of the Load Balancers from which the inbound traffic will be accepted.
    sourceTags : [str], default is Undefined, optional
        An array containing the names of Tags
        corresponding to groups of Droplets from which the inbound traffic
        will be accepted.
    """


    portRange?: str

    $protocol?: str

    sourceAddresses?: [str]

    sourceDropletIds?: [float]

    sourceKubernetesIds?: [str]

    sourceLoadBalancerUids?: [str]

    sourceTags?: [str]


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecForProviderOutboundRuleItems0:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall spec for provider outbound rule items0

    Attributes
    ----------
    destinationAddresses : [str], default is Undefined, optional
        An array of strings containing the IPv4
        addresses, IPv6 addresses, IPv4 CIDRs, and/or IPv6 CIDRs to which the
        outbound traffic will be allowed.
    destinationDropletIds : [float], default is Undefined, optional
        An array containing the IDs of
        the Droplets to which the outbound traffic will be allowed.
    destinationKubernetesIds : [str], default is Undefined, optional
        An array containing the IDs of
        the Kubernetes clusters to which the outbound traffic will be allowed.
    destinationLoadBalancerUids : [str], default is Undefined, optional
        An array containing the IDs
        of the Load Balancers to which the outbound traffic will be allowed.
    destinationTags : [str], default is Undefined, optional
        An array containing the names of Tags
        corresponding to groups of Droplets to which the outbound traffic will
        be allowed.
    portRange : str, default is Undefined, optional
        The ports on which traffic will be allowed
        specified as a string containing a single port, a range (e.g. "8000-9000"),
        or "1-65535" to open all ports for a protocol. Required for when protocol is
        tcp or udp.
    $protocol : str, default is Undefined, optional
        The type of traffic to be allowed.
        This may be one of "tcp", "udp", or "icmp".
    """


    destinationAddresses?: [str]

    destinationDropletIds?: [float]

    destinationKubernetesIds?: [str]

    destinationLoadBalancerUids?: [str]

    destinationTags?: [str]

    portRange?: str

    $protocol?: str


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    dropletIds : [float], default is Undefined, optional
        The list of the IDs of the Droplets assigned
        to the Firewall.
    dropletIdsRefs : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsRefsItems0], default is Undefined, optional
        References to Droplet in droplet to populate dropletIds.
    dropletIdsSelector : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsSelector, default is Undefined, optional
        droplet ids selector
    inboundRule : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderInboundRuleItems0], default is Undefined, optional
        The inbound access rule block for the Firewall.
        The inbound_rule block is documented below.
    name : str, default is Undefined, optional
        The Firewall name
    outboundRule : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderOutboundRuleItems0], default is Undefined, optional
        The outbound access rule block for the Firewall.
        The outbound_rule block is documented below.
    tags : [str], default is Undefined, optional
        The names of the Tags assigned to the Firewall.
    """


    dropletIds?: [float]

    dropletIdsRefs?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsRefsItems0]

    dropletIdsSelector?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsSelector

    inboundRule?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderInboundRuleItems0]

    name?: str

    outboundRule?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderOutboundRuleItems0]

    tags?: [str]


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsRefsItems0:
    r"""
    A Reference to a named object.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsRefsItems0Policy, default is Undefined, optional
        policy
    """


    name: str

    policy?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsRefsItems0Policy


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsRefsItems0Policy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsSelector:
    r"""
    Selector for a list of Droplet in droplet to populate dropletIds.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsSelectorPolicy


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderDropletIdsSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderInboundRuleItems0:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall spec init provider inbound rule items0

    Attributes
    ----------
    portRange : str, default is Undefined, optional
        The ports on which traffic will be allowed
        specified as a string containing a single port, a range (e.g. "8000-9000"),
        or "1-65535" to open all ports for a protocol. Required for when protocol is
        tcp or udp.
    $protocol : str, default is Undefined, optional
        The type of traffic to be allowed.
        This may be one of "tcp", "udp", or "icmp".
    sourceAddresses : [str], default is Undefined, optional
        An array of strings containing the IPv4
        addresses, IPv6 addresses, IPv4 CIDRs, and/or IPv6 CIDRs from which the
        inbound traffic will be accepted.
    sourceDropletIds : [float], default is Undefined, optional
        An array containing the IDs of
        the Droplets from which the inbound traffic will be accepted.
    sourceKubernetesIds : [str], default is Undefined, optional
        An array containing the IDs of
        the Kubernetes clusters from which the inbound traffic will be accepted.
    sourceLoadBalancerUids : [str], default is Undefined, optional
        An array containing the IDs
        of the Load Balancers from which the inbound traffic will be accepted.
    sourceTags : [str], default is Undefined, optional
        An array containing the names of Tags
        corresponding to groups of Droplets from which the inbound traffic
        will be accepted.
    """


    portRange?: str

    $protocol?: str

    sourceAddresses?: [str]

    sourceDropletIds?: [float]

    sourceKubernetesIds?: [str]

    sourceLoadBalancerUids?: [str]

    sourceTags?: [str]


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecInitProviderOutboundRuleItems0:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall spec init provider outbound rule items0

    Attributes
    ----------
    destinationAddresses : [str], default is Undefined, optional
        An array of strings containing the IPv4
        addresses, IPv6 addresses, IPv4 CIDRs, and/or IPv6 CIDRs to which the
        outbound traffic will be allowed.
    destinationDropletIds : [float], default is Undefined, optional
        An array containing the IDs of
        the Droplets to which the outbound traffic will be allowed.
    destinationKubernetesIds : [str], default is Undefined, optional
        An array containing the IDs of
        the Kubernetes clusters to which the outbound traffic will be allowed.
    destinationLoadBalancerUids : [str], default is Undefined, optional
        An array containing the IDs
        of the Load Balancers to which the outbound traffic will be allowed.
    destinationTags : [str], default is Undefined, optional
        An array containing the names of Tags
        corresponding to groups of Droplets to which the outbound traffic will
        be allowed.
    portRange : str, default is Undefined, optional
        The ports on which traffic will be allowed
        specified as a string containing a single port, a range (e.g. "8000-9000"),
        or "1-65535" to open all ports for a protocol. Required for when protocol is
        tcp or udp.
    $protocol : str, default is Undefined, optional
        The type of traffic to be allowed.
        This may be one of "tcp", "udp", or "icmp".
    """


    destinationAddresses?: [str]

    destinationDropletIds?: [float]

    destinationKubernetesIds?: [str]

    destinationLoadBalancerUids?: [str]

    destinationTags?: [str]

    portRange?: str

    $protocol?: str


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecProviderConfigRefPolicy


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecProviderConfigRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsTo:
    r"""
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToConfigRef

    metadata?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToMetadata

    name: str


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToConfigRef:
    r"""
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToConfigRefPolicy


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToConfigRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecPublishConnectionDetailsToMetadata:
    r"""
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatus:
    r"""
    FirewallStatus defines the observed state of Firewall.

    Attributes
    ----------
    atProvider : NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProvider

    conditions?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusConditionsItems0]

    observedGeneration?: int


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProvider:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall status at provider

    Attributes
    ----------
    createdAt : str, default is Undefined, optional
        A time value given in ISO8601 combined date and time format
        that represents when the Firewall was created.
    dropletIds : [float], default is Undefined, optional
        The list of the IDs of the Droplets assigned
        to the Firewall.
    id : str, default is Undefined, optional
        A unique ID that can be used to identify and reference a Firewall.
    inboundRule : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderInboundRuleItems0], default is Undefined, optional
        The inbound access rule block for the Firewall.
        The inbound_rule block is documented below.
    name : str, default is Undefined, optional
        The Firewall name
    outboundRule : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderOutboundRuleItems0], default is Undefined, optional
        The outbound access rule block for the Firewall.
        The outbound_rule block is documented below.
    pendingChanges : [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderPendingChangesItems0], default is Undefined, optional
        An list of object containing the fields, "droplet_id",
        "removing", and "status".  It is provided to detail exactly which Droplets
        are having their security policies updated.  When empty, all changes
        have been successfully applied.
    status : str, default is Undefined, optional
        A status string indicating the current state of the Firewall.
        This can be "waiting", "succeeded", or "failed".
    tags : [str], default is Undefined, optional
        The names of the Tags assigned to the Firewall.
    """


    createdAt?: str

    dropletIds?: [float]

    id?: str

    inboundRule?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderInboundRuleItems0]

    name?: str

    outboundRule?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderOutboundRuleItems0]

    pendingChanges?: [NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderPendingChangesItems0]

    status?: str

    tags?: [str]


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderInboundRuleItems0:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall status at provider inbound rule items0

    Attributes
    ----------
    portRange : str, default is Undefined, optional
        The ports on which traffic will be allowed
        specified as a string containing a single port, a range (e.g. "8000-9000"),
        or "1-65535" to open all ports for a protocol. Required for when protocol is
        tcp or udp.
    $protocol : str, default is Undefined, optional
        The type of traffic to be allowed.
        This may be one of "tcp", "udp", or "icmp".
    sourceAddresses : [str], default is Undefined, optional
        An array of strings containing the IPv4
        addresses, IPv6 addresses, IPv4 CIDRs, and/or IPv6 CIDRs from which the
        inbound traffic will be accepted.
    sourceDropletIds : [float], default is Undefined, optional
        An array containing the IDs of
        the Droplets from which the inbound traffic will be accepted.
    sourceKubernetesIds : [str], default is Undefined, optional
        An array containing the IDs of
        the Kubernetes clusters from which the inbound traffic will be accepted.
    sourceLoadBalancerUids : [str], default is Undefined, optional
        An array containing the IDs
        of the Load Balancers from which the inbound traffic will be accepted.
    sourceTags : [str], default is Undefined, optional
        An array containing the names of Tags
        corresponding to groups of Droplets from which the inbound traffic
        will be accepted.
    """


    portRange?: str

    $protocol?: str

    sourceAddresses?: [str]

    sourceDropletIds?: [float]

    sourceKubernetesIds?: [str]

    sourceLoadBalancerUids?: [str]

    sourceTags?: [str]


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderOutboundRuleItems0:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall status at provider outbound rule items0

    Attributes
    ----------
    destinationAddresses : [str], default is Undefined, optional
        An array of strings containing the IPv4
        addresses, IPv6 addresses, IPv4 CIDRs, and/or IPv6 CIDRs to which the
        outbound traffic will be allowed.
    destinationDropletIds : [float], default is Undefined, optional
        An array containing the IDs of
        the Droplets to which the outbound traffic will be allowed.
    destinationKubernetesIds : [str], default is Undefined, optional
        An array containing the IDs of
        the Kubernetes clusters to which the outbound traffic will be allowed.
    destinationLoadBalancerUids : [str], default is Undefined, optional
        An array containing the IDs
        of the Load Balancers to which the outbound traffic will be allowed.
    destinationTags : [str], default is Undefined, optional
        An array containing the names of Tags
        corresponding to groups of Droplets to which the outbound traffic will
        be allowed.
    portRange : str, default is Undefined, optional
        The ports on which traffic will be allowed
        specified as a string containing a single port, a range (e.g. "8000-9000"),
        or "1-65535" to open all ports for a protocol. Required for when protocol is
        tcp or udp.
    $protocol : str, default is Undefined, optional
        The type of traffic to be allowed.
        This may be one of "tcp", "udp", or "icmp".
    """


    destinationAddresses?: [str]

    destinationDropletIds?: [float]

    destinationKubernetesIds?: [str]

    destinationLoadBalancerUids?: [str]

    destinationTags?: [str]

    portRange?: str

    $protocol?: str


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusAtProviderPendingChangesItems0:
    r"""
    networking digitalocean crossplane io v1alpha1 firewall status at provider pending changes items0

    Attributes
    ----------
    dropletId : float, default is Undefined, optional
        A unique ID that can be used to identify and reference a Firewall.
    removing : bool, default is Undefined, optional
        removing
    status : str, default is Undefined, optional
        A status string indicating the current state of the Firewall.
        This can be "waiting", "succeeded", or "failed".
    """


    dropletId?: float

    removing?: bool

    status?: str


schema NetworkingDigitaloceanCrossplaneIoV1alpha1FirewallStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


