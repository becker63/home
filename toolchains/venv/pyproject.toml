[project]
name = "myproject"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cloudcoil",
    "datamodel-code-generator>=0.42.2",
    "filelock>=3.20.0",
    "pytest>=9.0.2",
    "typer>=0.20.0",
]  # shared deps for both entrypoints
